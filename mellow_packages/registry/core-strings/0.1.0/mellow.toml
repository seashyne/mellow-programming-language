name = "core-strings"
version = "0.1.0"
description = "String helpers built on top of the host string module."
entry = "src/main.mel"
license = "MIT"
visibility = "public"

authors = ["Mellow Code Team"]
keywords = ["core", "strings", "text", "formatting", "starter"]
badges = ["official"]
official = true
[dependencies]
string = "host"
