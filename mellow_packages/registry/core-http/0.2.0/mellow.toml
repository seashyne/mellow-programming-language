name = "core-http"
version = "0.2.0"
description = "HTTP request and route descriptors for host integrations."
entry = "src/main.mellow"
license = "MIT"
visibility = "public"

authors = ["Mellow Code Team"]
keywords = ["core", "http", "api", "route", "starter"]
badges = ["official"]
official = true
[dependencies]
