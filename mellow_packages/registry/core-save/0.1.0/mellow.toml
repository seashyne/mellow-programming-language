name = "core-save"
version = "0.1.0"
description = "Small persistence helpers using built-in save/load APIs."
entry = "src/main.mel"
license = "MIT"
visibility = "public"

authors = ["Mellow Code Team"]
keywords = ["core", "save", "storage", "compatibility", "deprecated"]
badges = ["official", "deprecated"]
official = true
deprecated = true
[dependencies]
# no external dependencies
