name = "core-storage"
version = "0.1.0"
description = "Save/load helpers for local app and game data."
entry = "src/main.mellow"
license = "MIT"
keywords = ["core", "storage", "save"]

[dependencies]
storage = "host"
