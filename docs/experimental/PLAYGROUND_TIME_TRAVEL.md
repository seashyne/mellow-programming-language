# Playground Time Travel

Run locally:

```bash
mellow playground
```

Features:
- click editor gutter to toggle breakpoints
- share sessions that persist on disk
- record each run and replay it later from the saved recording id
- inspect per-step watch diffs in the debugger panel
