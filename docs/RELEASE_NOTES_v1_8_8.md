# Mellow 1.8.8

- HTTP webhook server via `mellow agent webhook serve`
- durable queue log in `.mellow/scheduler/queue_log.jsonl`
- retry and dead-letter queue support
- queue dead-letter inspection and requeue commands
