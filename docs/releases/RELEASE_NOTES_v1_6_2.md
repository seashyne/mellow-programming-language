# Mellow 1.6.2

- Namespace UX improvements for `mellow add`
- Autocomplete-style package selection for `@owner/pkg` and short names
- Auto-install on missing import during runtime resolution
- Better suggestions when an import cannot be resolved
