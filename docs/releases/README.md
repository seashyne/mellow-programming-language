# Release Notes Archive

This directory keeps per-version Mellow release notes.

Current docs should link to the latest release notes or the changelog summary,
while historical version files stay here to keep the top-level `docs/` directory
easy to scan.
