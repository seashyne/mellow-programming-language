# MellowLang v1.5.2 Release Notes

## Highlights
- Online package registry server with publish/install/search APIs
- CLI support for remote login, search, publish, install, registry config, and local registry serving
- `mellow.toml` manifest support in addition to `mellow.pkg.json`
- Package archive SHA-256 hashing
- Full package registry spec added for implementation and scaling
