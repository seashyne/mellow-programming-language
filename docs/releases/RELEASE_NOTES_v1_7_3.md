# Mellow 1.7.3

- Agent signing via SHA256 manifest signatures with `--signing-key` and `--verify-key`
- Version constraints for agent package dependencies in `[dependencies]`
- Recursive install with dependency validation
- Dependency graph command: `mellow agent package graph <ref>`
