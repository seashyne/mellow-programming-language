# MellowLang v1.2.4

- Add script-controlled storage base directory: `storage_dir(path)`
- Add `storage_pwd()` and `storage_info()` to introspect current storage base
- Add `--unsafe-fs` to allow absolute paths / '..' in `storage_dir` (unsafe)
