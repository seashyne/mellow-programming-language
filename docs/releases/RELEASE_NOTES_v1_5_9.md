# Mellow 1.5.9

- project runtime resolver (`resolve-runtime`)
- dependency `update` and `uninstall` commands
- private packages for the Cloudflare registry
- package import sugar: `use core-ai as ai` and `need core-ai as ai`
- runtime package import resolution for `pkg:` imports
