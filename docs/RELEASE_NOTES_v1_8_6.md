
# Mellow 1.8.6

## Added
- Scheduled agent jobs with cron expressions
- Cron workflow execution
- Background runner status and execution loop
- CLI commands:
  - `mellow agent schedule add`
  - `mellow agent schedule list`
  - `mellow agent schedule run-due`
  - `mellow agent runner start`
  - `mellow agent runner status`
