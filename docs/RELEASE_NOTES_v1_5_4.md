# MellowLang v1.5.4

- Stabilized registry/login CLI flow.
- Added top-level `mellow registry <url>` and `mellow logout`.
- Improved default help to show registry commands.
- Added local launcher scripts for Windows that run from project source.
- Added clearer login error hint for token/salt mismatch.
