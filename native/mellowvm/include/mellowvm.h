#ifndef MELLOWVM_H
#define MELLOWVM_H

// Minimal CPython-extension VM loop for MellowLang (Hybrid mode).
// NOTE: This is intentionally small in v1.2.5-hybrid: it accelerates the hot
// dispatch loop while keeping values as PyObject* for maximum compatibility.

#endif // MELLOWVM_H
