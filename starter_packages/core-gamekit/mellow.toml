name = "core-gamekit"
version = "0.1.0"
description = "Scene, movement, easing, and path helpers for simple games."
entry = "src/main.mel"
license = "MIT"
visibility = "public"
keywords = ["game", "starter", "2d", "mellow"]

[dependencies]
game = "host"
math = "host"
