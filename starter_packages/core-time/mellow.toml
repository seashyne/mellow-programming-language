name = "core-time"
version = "0.2.1"
description = "Time helpers for timestamps and lightweight elapsed measurements."
entry = "src/main.mel"
license = "MIT"
visibility = "public"

authors = ["Mellow Core Team"]
keywords = ["core", "time", "clock", "timestamp", "starter"]
badges = ["official"]
official = true
[dependencies]
time = "host"
