name = "core-time"
version = "0.1.0"
description = "Time helpers for timestamps and lightweight elapsed measurements."
entry = "src/main.mel"
license = "MIT"
visibility = "public"
keywords = ["core", "time", "clock", "starter"]

[dependencies]
time = "host"
