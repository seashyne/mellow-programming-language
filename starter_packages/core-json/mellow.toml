name = "core-json"
version = "0.2.1"
description = "JSON encode/decode wrappers for app and API scripts."
entry = "src/main.mel"
license = "MIT"
visibility = "public"

authors = ["Mellow Core Team"]
keywords = ["core", "json", "data", "decode", "starter"]
badges = ["official"]
official = true
[dependencies]
json = "host"
