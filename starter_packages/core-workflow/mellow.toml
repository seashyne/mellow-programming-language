name = "core-workflow"
version = "0.2.1"
description = "Starter helpers for event payloads, retries, and simple jobs."
entry = "src/main.mel"
license = "MIT"
visibility = "public"

authors = ["Mellow Core Team"]
keywords = ["core", "workflow", "jobs", "automation", "starter"]
badges = ["official"]
official = true
[dependencies]
time = "host"
json = "host"
