name = "core-workflow"
version = "0.1.0"
description = "Starter helpers for event payloads, retries, and simple jobs."
entry = "src/main.mel"
license = "MIT"
visibility = "public"
keywords = ["workflow", "jobs", "starter", "automation"]

[dependencies]
time = "host"
json = "host"
