name = "core-storage"
version = "0.2.1"
description = "Persistence helpers using Mellow save/load statements."
entry = "src/main.mellow"
license = "MIT"
visibility = "public"

authors = ["Mellow Core Team"]
keywords = ["core", "storage", "persistence", "save", "starter"]
badges = ["official"]
official = true
[dependencies]
